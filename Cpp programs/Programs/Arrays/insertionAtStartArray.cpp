// Program to insert an element at the start of the array
#include<iostream>
using namespace std;
int main(){
    int i,j,n,newvalue;

    cout<<"Enter the size of array ="<<endl;
    cin>>n;

    int array[n];
    cout<<"Enter the values in the array = "<<endl;
    for(i=0 ; i,n , i++){
        cin>>array[i];
    }

    cout<<"Enter the value which have to be inserted at the start =";
    cin>>newvalue;
    for (i=(n-1) ; )



    cout<<"The new array is as follows ="<<endl;
    for(i=0 ; i<(n+1) ;i++){
        cout<<array[i]<<" ";2
    }cout<<endl;
    return 0;
}